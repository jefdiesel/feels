-- Revert seed profile updates (best effort - some data loss expected)

-- Revert wants_kids (can't perfectly restore original values)
-- No action needed - values are still valid

-- Revert alcohol (can't distinguish original rarely from socially)
-- No action needed - values are still valid

-- Revert weed (can't distinguish original rarely from socially)
-- No action needed - values are still valid

-- IDs in prompts don't need to be removed
-- No action needed
