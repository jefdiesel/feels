{
  "$schema": "https://schema.railpack.com",
  "provider": "golang",
  "steps": {
    "build": {
      "commands": ["go build -o server ./cmd/server"]
    }
  },
  "deploy": {
    "startCommand": "./server",
    "inputs": [{ "step": "build", "include": ["."] }]
  }
}
